services:
  swarm-sim:
    environment:
      DISPLAY: ${DISPLAY:-:0}
      DRONE_HEADLESS: "false"
      DRONE_USE_RVIZ: "true"
      LIBGL_ALWAYS_SOFTWARE: ${LIBGL_ALWAYS_SOFTWARE:-0}
      QT_X11_NO_MITSHM: "1"
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
